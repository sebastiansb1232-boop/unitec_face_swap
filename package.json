{
  "name": "unitec-panoptico-demo",
  "version": "1.0.0",
  "description": "Demo educativa de UNITEC sobre riesgos de permisos de camara y datos biometricos en la web",
  "main": "server.js",
  "scripts": {
    "build": "node scripts/generate-config.js",
    "start": "node server.js",
    "dev": "node scripts/generate-config.js && npx http-server public -p 8000"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
